# LINHAC 2026

Faceoff.....

## Run

### Filter python script
To run filter_events.py:

* pip install -r requirements.txt

run the filterevent, right now it is configured to name your csv file "1" but can be easily changed by changing variable name FILE_NAME.

Script will output file "post_faceoff_events.csv" with all relevant events (all faceoffs and 10 seconds after)